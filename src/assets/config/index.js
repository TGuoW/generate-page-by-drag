import Button from './Button'

export default {
    Button
}
{/* <el-form-item label="按钮类型">
<el-select on-input={(e) => this.sync('type', e)} value={this.type} placeholder="请选择">
  { this.typeSelectArr.map(item =>
      <el-option
        key={item.value}
        label={item.label}
        value={item.value}>
      </el-option>
    )
  }
</el-select>
</el-form-item>
<el-form-item label="朴素按钮">
<el-switch
  on-input={(e) => this.sync('isPlain', e)}
  value={this.isPlain}
  active-text="是"
  inactive-text="否">>
</el-switch>
</el-form-item>
<el-form-item label="圆角">
<el-switch
  on-input={(e) => this.sync('isRound', e)}
  value={this.isRound}
  active-text="是"
  inactive-text="否">>
</el-switch>
</el-form-item>
<el-form-item label="圆形">
<el-switch
  on-input={(e) => this.sync('isCircle', e)}
  value={this.isCircle}
  active-text="是"
  inactive-text="否">>
</el-switch>
</el-form-item>
<el-form-item label="禁用">
<el-switch
  on-input={(e) => this.sync('isDisabled', e)}
  value={this.isDisabled}
  active-text="是"
  inactive-text="否">>
</el-switch>
</el-form-item>
<el-form-item label="默认聚焦">
<el-switch
  on-input={(e) => this.sync('isAutofocus', e)}
  value={this.isAutofocus}
  active-text="是"
  inactive-text="否">>
</el-switch>
</el-form-item> */}